# О файле

Это рабочие инструкции для AI-ассистента (Claude Code) и краткая дока проекта.
Стек: Python, aiogram 3.x, SQLAlchemy 2.x (async), APScheduler.

---

# ⚙️ Репозиторий и рабочий процесс (читать первым)

**GitHub:** https://github.com/petershor99-qa/bottennis — публичный репозиторий, CI на GitHub Actions (pytest на каждый push/PR, файл `.github/workflows/tests.yml`)

**VPS:** ✅ переезд завершён. Бот работает на новом сервере FirstVDS (Нидерланды, Ubuntu, systemd-сервис `bottennis`, рабочая папка `/opt/bottennis`). Старый сервер (`188.120.228.39`) выведен из эксплуатации — не достукивался до Telegram API. При переезде: перевыпущен BOT_TOKEN, обновлены секреты CI (`VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY`). Сам хост/IP нового сервера в публичный репозиторий не вписываем — он только в секретах GitHub Actions. **Git-репозиторий на самом VPS клонирован по SSH** (`git@github.com:...`, отдельный read-only deploy key `~/.ssh/bottennis_deploy_key`, прописан в `~/.ssh/config` — не путать с `VPS_SSH_KEY`, тем ключом GitHub Actions заходит НА VPS, это разные ключи разных направлений), не по анонимному HTTPS — см. заметку про 401 в конце файла.

---

## Ветки (две ветки)

```
main       ← прод. Только через PR из develop. Прямой пуш заблокирован, CI обязателен.
  └── develop  ← рабочая ветка. Здесь идёт вся разработка.
```

**Правила:**
- В `main` — никогда не коммитишь напрямую. Только PR из `develop` (с зелёным CI).
- В `develop` — мелкие правки напрямую, крупные фичи через `feature/`-ветки + PR.
- Всегда работаешь на `develop` или `feature/xxx`.

---

## Флоу: добавить новую фичу (по порядку)

**Шаг 1 — Создай ветку для задачи:**
```bash
git checkout develop
git pull
git checkout -b feature/название-задачи
```
Например: `feature/add-tournament-mode`, `feature/fix-rating-bug`

**Шаг 2 — Работаешь с Клодом, пишешь код.**

**Шаг 3 — Прогони тесты локально:**
```bash
py -3.13 -m pytest -q
```
Упали → чинишь, дальше не идёшь.

**Шаг 4 — Коммит и пуш:**
```bash
git add -A && git commit -m "feat: описание что сделал"
git push origin feature/название-задачи
```

**Шаг 5 — PR на GitHub:** `feature/xxx` → `develop`
- Открываешь на github.com
- GitHub Actions автоматически прогонит тесты
- Мержишь

**Шаг 6 — Когда готов к релизу:** PR `develop` → `main`, дождись зелёного CI, мержишь

**Шаг 7 — Деплой в прод:** автоматический ✅ — мерж в `main` запускает job `deploy` (после зелёных `lint`+`pytest`), который сам делает `git pull` + `systemctl restart bottennis` на VPS. Ручное вмешательство не нужно.

> Запасной ручной деплой (если автодеплой недоступен). Хост — из секрета `VPS_HOST` (в публичный репо не пишем):
> ```powershell
> ssh root@<VPS_HOST> "cd /opt/bottennis && git pull && systemctl restart bottennis"
> ```

**Шаг 8 — Обновить** `RELEASE_NOTES.md` + Mionika (см. ниже)

---

⚠️ **Репозиторий публичный.** Не вписывать реальные имена игроков, токены, персональные данные. В `RATING_ANALYSIS.md` имена обезличены (Игрок A–F) — так и держим. `.env` и `*.db` в `.gitignore`, не коммитим.

---

# Проект

Telegram-бот для рейтинговых игр в настольный теннис внутри закрытой группы.
Регистрация только по инвайт-коду. Один разработчик. Стадия: MVP в проде на VPS (FirstVDS).

**Что умеет бот:**
- Регистрация игроков по инвайт-ссылке (`/start <INVITE_CODE>`)
- Вызов соперника на матч (матч начинается сразу, без шага принятия)
- Внесение результата: пошаговый FSM-ввод или прямой ввод счёта в чат (`11:7 9:11 11:5`)
- Отмена принятого матча любым из участников
- Автопересчёт рейтинга после каждого матча
- Таблица рейтинга (с ▲▼ за неделю), личная статистика, история матчей, личные встречи (H2H)
- **Босс-файт за 1-е место** — #1 занимается не по очкам, только победой над текущим чемпионом (`Player.is_champion`) в отдельном матче: ×2 к дельте, ничья запрещена, блокировка мгновенного реванша, авто-освобождение трона при 7 днях бездействия чемпиона
- Достижения (52 ачивки, 24 скрытых до разблокировки, сгруппированы по категориям на экране «Достижения») + пасхалки после матчей + прогресс до ближайшей счётной ачивки в статистике
- Личные рекорды (7 метрик, сравнение только с собственной историей игрока — не с другими) после матча
- Звания по диапазону рейтинга (Джун/Миддл/Сеньор/Тим лид/Ген дир) — косметика в статистике и профиле
- Зал славы — полная история правлений на месте #1 (кнопка на экране «Рейтинг»)
- Карточка «поделиться победой» — текстовая карточка со счётом и репортажем, кнопка на экране результата (только у реального победителя)
- Постоянная клавиатура снизу — Вызвать на матч / Рейтинг / Статистика, всегда под строкой ввода
- График динамики рейтинга (quickchart.io)
- Тепловая карта активности за 90 дней (личная/клубная, переключатель кнопкой) — экран «Статистика»
- Рекорды клуба, матрица доминирования и индекс формы (кнопки на экране рейтинга) — индекс формы ранжирует по последним 10 матчам, а не по всей карьере
- Экран «Мои матчи»: активные матчи клуба + «С кем сыграть?» с рекомендациями (`get_rec_signal`)
- Только один активный матч на игрока одновременно + напоминание через 24 часа, если матч завис
- Итоги дня каждый вечер в 21:30 МСК (топ дня + «матч дня»)
- Еженедельный дайджест каждый понедельник в 9:00 МСК + итоги месяца 1-го числа в 10:00 МСК + итоги квартала 1-го числа января/апреля/июля/октября в 10:30 МСК + итоги года 31 декабря в 14:00 МСК
- Ежемесячный offsite-бэкап БД админу в личку (1-го числа 10:15 МСК) + `/backup` для бэкапа по запросу (админ)
- `/feedback` — отправить идею/баг напрямую разработчику, доступна всем игрокам

---

# Карта файлов

| Файл | Что делает |
|------|-----------|
| `main.py` | Точка входа: Bot, Dispatcher, middleware, роутеры, планировщик |
| `bot/handlers/start.py` | `/start`, `/cancel`, `/help`, `/feedback` (доступна всем, не только ADMIN_ID), `/fix_rating`, `back_to_menu` |
| `bot/handlers/leaderboard.py` | Рейтинг-таблица (`menu_leaderboard`, билдер `_build_leaderboard_screen` — общий с постоянной клавиатурой), Сегодня (`menu_today`), Рекорды клуба (`club_records`), Матрица доминирования (`dominance_matrix`), Индекс формы (`form_index` — ранжирует по последним `FORM_WINDOW`=10 матчам игрока, не по всей карьере), Зал славы (`hall_of_fame` — полная история `ChampionReign`) |
| `bot/handlers/profile.py` | Статистика (`menu_stats`, билдер `_build_stats_screen`) с прогрессом до ачивки, профиль игрока, достижения |
| `bot/handlers/history.py` | История матчей, график рейтинга, тепловая карта активности (`activity_heatmap_me`/`_club`, v2.107.0), экран «Мои матчи» (`menu_matches`: активные матчи клуба + «С кем сыграть?»), личные встречи H2H (`h2h_`) |
| `bot/handlers/challenge.py` | Вызов (матч сразу active, детект босс-файта `is_boss_fight`, кнопка «БОСС-ФАЙТ», билдер `_build_challenge_screen` — общий с постоянной клавиатурой), отмена матча с подтверждением |
| `bot/handlers/match_result.py` | FSM ввода результата (счёт партий → подтверждение → сохранение); ×2/перенос трона/блок ничьей в боссфайте; карточка «поделиться победой» (`send_share_card`) |
| `bot/handlers/admin.py` | Админ-команды: `/dbstats`, `/myid`, `/backup` (бэкап БД по запросу) |
| `bot/keyboards/inline.py` | Все inline-клавиатуры проекта + `main_reply_kb()` — постоянная клавиатура снизу |
| `bot/services/rating.py` | Формула ELO с бонусом за доминирование |
| `bot/services/stats.py` | `_compute_player_stats` (+ `sets_won`/`career_points` для вех), `_nearest_achievement_progress` — общие для profile.py и еженедельного дайджеста (scheduler.py), вынесены из profile.py, чтобы хендлер не импортировал напрямую из другого хендлера |
| `bot/services/personal_records.py` | Личные рекорды (v2.100.0) — `check_personal_records_on_win`/`_on_loss`/`_on_draw`, флавор-текст считается заново из истории на каждое подтверждение результата; `backfill_personal_records()` (v2.106.0) — восстановление истории дат из `PersonalRecordEarned` |
| `bot/db/models.py` | Модели Player (+`is_champion`), Match (+`is_boss_fight`), MatchStatus, `ChampionReign` (журнал правлений на #1), `AchievementEarned`/`PersonalRecordEarned` (v2.106.0 — даты) |
| `bot/db/database.py` | Engine, async_session, init_db (бэкфилл ачивок + личных рекордов + `bootstrap_champion`) |
| `bot/middleware.py` | DatabaseMiddleware — открывает сессию на каждый апдейт |
| `bot/scheduler.py` | Напоминание о незавершённом матче (раз в час, от 24ч) + авто-освобождение трона (раз в час, от 7 дней) + итоги дня (21:30) / недели (пн 9:00) / месяца (1-го 10:00) / квартала (1-го января/апреля/июля/октября 10:30) / года (31 декабря 14:00) + offsite-бэкап БД (`send_backup_file`, общий с `/backup`). Хелперы `_most_played_pair`, `_longest_streak`, `_quarter_bounds_msk`, `_year_bounds_msk` |
| `bot/states/states.py` | FSM-состояния ввода результата |
| `bot/services/validation.py` | Валидация счёта партии по правилам настольного тенниса |
| `bot/utils.py` | `get_player`, `match_rating_delta`, `pluralize_matches`/`pluralize_wins`/`pluralize_days`, `_match_line`, H2H (`compute_h2h`), «матч дня» (`match_drama_score`/`pick_match_of_day`/`match_drama_reason`/`match_report`), босс-файт (`get_champion`/`get_challenger`/`get_champion_and_challenger`/`bootstrap_champion`/`try_transfer_champion`/`boss_fight_rematch_blocked`/`longest_champion_reign`), `notify_all_players`, `rank_title` (звания по рейтингу) |

---

# Бизнес-правила (не нарушать)

- **Результат может внести любой участник матча.** Если репортёр ввёл счёт, где он проиграл, бот инвертирует перспективу и корректно записывает победу соперника (`sets_data` всегда хранится в перспективе победителя; при ничьей — в перспективе challenger).
- **Рейтинг никогда не сбрасывается.** Только накапливается через `+delta` / `-delta` после каждого матча. Пол рейтинга: **1000.0** для новичков (до 15 матчей), **900.0** для ветеранов (15+ матчей). Стартовый рейтинг: 1000.0.
- **Рейтинг отображается с одним знаком после запятой** везде (не `int`, а `round(x, 1)`).
- **Счёт партии валидируется** по правилам настольного тенниса: победитель ≥11 очков с отрывом ≥2. Дьюс: 12:10, 13:11 и т.д.
- **У игрока может быть только ОДИН активный матч одновременно** (стол один, матчи строго последовательные) — единый источник правды `get_active_match(session, player_id)` в `utils.py`, используется в 5 местах: `show_players_for_challenge`/`send_challenge` (challenge.py — заранее прячет из списка вызова всех занятых игроков и авторитетно блокирует на момент вызова, страхует от гонок и от кнопки «Реванш», которая идёт в обход списка), `show_player_profile`/`h2h_kb`-звонок в `show_h2h` (прячут кнопку «⚔️ Вызвать», если занят зритель ИЛИ владелец профиля/соперник по H2H — раньше проверялась только пара «есть ли активный матч именно между этими двумя», из-за чего кнопка вела в тупик), `show_my_matches` (history.py — если занят зритель, кнопки «Вызвать X» в блоке «С кем сыграть?» не показываются вообще, хотя инфо-строки по соперникам остаются). Старый точечный кейс «уже есть активный матч именно с этим соперником» — частный случай общего правила, отдельно не проверяется.
- **Напоминание о незавершённом матче** (`send_match_reminders`, раз в час, порог 24ч от `accepted_at`/fallback `created_at`) — возвращено в v2.80.0 после того, как «один активный матч одновременно» сделало забытый матч блокирующим ОБОИХ участников (раньше висел незаметно, поэтому напоминание убрали как шум в v2.70.0). Флаг `Match.reminder_sent` шлёт один раз на матч. Сообщение — с кнопкой `busy_with_match_kb` (внести результат сразу / отменить).
- **Активный матч можно отменить** любым из двух участников — оба получают уведомление.
- **Босс-файт за 1-е место** (с v2.84.0, харденинг в v2.85.0). Чемпион — `Player.is_champion` (единственный `True` в клубе), не пересчитывается на лету. Если чемпион не назначен ни у кого — фича полностью неактивна: сортировка чисто по рейтингу, без кнопок/эмодзи/уведомлений (`bootstrap_champion` в `init_db()` фиксирует текущего лидера при первом старте после апдейта, без порога матчей — это фиксация положения, не передача власти; тот же флаг служит ручным рубильником — `UPDATE players SET is_champion=0`). Факт «фича когда-либо активировалась» хранится отдельно, в `ChampionReign` (ниже) — иначе `bootstrap_champion` переизбирал бы чемпиона заново на каждом деплое/рестарте, игнорируя ручное отключение. **Претендент** — `get_challenger()`, вычисляется на лету (не хранится): не чемпион, рейтинг строго выше чемпионского, ≥`NEWCOMER_THRESHOLD` (15) завершённых матчей; если порог не набран — претендента НЕТ ВООБЩЕ, право не откатывается на следующего по рейтингу. Пара (чемпион, текущий претендент) автоматически становится боссфайтом при создании матча (`Match.is_boss_fight`) независимо от способа вызова. Ничья запрещена — проверка в `finish_sets()`, страховка в `confirm_result()` (не только в трёх местах ввода счёта — все сходятся к одной из этих двух точек). Дельта ×2 (`BOSS_FIGHT_MULT`), `repeat_multiplier` не применяется. Победил претендент → трон переходит; победил чемпион → не меняется (обе ветки — в `confirm_result()`, match_result.py). Реванш между парой заблокирован (`boss_fight_rematch_blocked()`), пока НЕЧЕМПИОН пары (текущий, не исторический — переопределяется живьём через `get_champion()`) не завершит матч с третьим игроком; матчи текущего чемпиона пары блок не снимают; отменённый (`declined`) боссфайт блок не включает — проверяется и в `send_challenge` (авторитетно), и в видимости кнопок (`can_challenge` в profile.py/history.py, список/ярлык в challenge.py — та же логика, что уже применена к `get_active_match`, иначе кнопка ведёт в тупик). Авто-освобождение трона — та же часовая джоба, что и напоминания о матчах (`check_champion_auto_release`, scheduler.py): чемпион не завершал матчей >7 дней → трон топу по очкам среди игроков с ≥15 матчей, без боя; кандидатов нет — трон остаётся; **пропускает цикл, если у чемпиона сейчас есть активный матч** (в т.ч. собственный боссфайт) — иначе трон мог гонкой достаться постороннему прямо во время чужого поединка за него. Смена `Player.is_champion` — **только через `try_transfer_champion()`** (CAS: `UPDATE ... WHERE is_champion=True AND id=<from>`, тот же паттерн, что у `Match.status`) в обоих местах переноса (авто-освобождение и `confirm_result`) — если трон уже сменился где-то ещё, перенос тихо отменяется вместо порчи данных (без этого второй чемпион одновременно ронял бы `get_champion()` с `MultipleResultsFound`). Race condition в `send_challenge` (см. «Известные ограничения» ниже, про создание дублирующихся матчей) — распространяется и на боссфайт, отдельно от гонки за трон, сознательно не чинили.
- **`ChampionReign`** (с v2.85.0) — журнал периодов владения #1: `player_id`, `started_at`, `ended_at` (`None` = текущее правление). Открывается/закрывается вместе с любой сменой чемпиона (`bootstrap_champion`, `try_transfer_champion`). Питает рекорд клуба «Дольше всех лидировал» (`longest_champion_reign()`) и служит признаком «фича когда-либо инициализировалась» для рубильника выше — отдельно от текущего значения `Player.is_champion`, которое можно обнулить руками.
- **MatchStatus**: `accepted` → `completed` (или `declined` при отмене). `pending` сохранён в enum только для совместимости со старыми записями БД — новые матчи создаются сразу как `accepted`.
- **Смена статуса матча — только атомарным CAS-UPDATE** (`UPDATE ... WHERE status='accepted'` + проверка `rowcount`) — защита от гонок: двойной тап «Всё верно», отмена vs внесение результата. После CAS текущий матч уже `completed` — в подсчётах завершённых матчей игрока исключать его через `Match.id != match_id`.
- **Граница «сегодня» — полночь по МСК** через `msk_day_start()` из `utils.py` (не UTC) — единая для экрана «Сегодня», итогов дня, пасхалок **и дневных ачивок** («Маньячелло», «Неистого» — с v2.58.0).
- **Конфиг читается из env на уровне модулей** (start.py, admin.py, database.py), поэтому `load_dotenv()` в main.py обязан стоять ДО импортов бота (ruff: per-file-ignore E402). Целочисленные env-переменные читать только через `env_int()` из utils — `int(os.getenv(...))` падает на пустом значении.
- **Игроки с 0 сыгранных матчей скрыты из рейтинг-таблицы и матрицы доминирования** (с v2.59.0) — пустые регистрации не засоряют рейтинг. На экране «С кем сыграть?» такой игрок остаётся видимым и вызываемым с пометкой «🆕 ещё не играл» — иначе новичок не смог бы получить первый матч.
- **График динамики рейтинга** доступен для себя (`rating_chart`) и для любого игрока из его профиля (`player_chart_{id}`) — общий хелпер `_send_rating_chart` в history.py. Предыдущий график в чате удаляется при построении нового (последний message_id хранится в памяти по chat_id, `_last_chart_msg`). **Граница оси Y — динамическая** (`_rating_axis_bounds()` в utils.py, с v2.88.0): min/max/шаг сетки считаются от фактического разброса значений конкретного графика с отступом, а не зашиты фиксированно (0..1400) — иначе типичный разброс рейтинга 900..1400 сжимался бы в верхнюю треть картинки, а любой будущий рост рейтинга выше жёстко заданного потолка обрезал бы график. Пунктирная линия-ориентир на 1000 (стартовый/новичковый пол) всегда включена в диапазон оси, даже если рейтинг игрока постоянно сильно выше/ниже неё.
- **Мемная фраза под прогнозом** на экране матча (`match_phrase` в utils.py) подбирается по шансам СМОТРЯЩЕГО: фаворит (>65%), равны (35–65%), андердог (<35%). Стабильна в рамках матча — индекс по `match.id`, чтобы не «прыгала» при перерисовке. Пулы фраз: `FAVORITE_PHRASES` / `EVEN_PHRASES` / `UNDERDOG_PHRASES`.
- **«Дерби клуба»** в рекордах клуба — самая играющая пара (по числу очных завершённых матчей, от 2+).
- **Рекорды клуба** (`show_club_records`) кроме дерби: «Высший рейтинг в истории» (max `peak_rating` среди игравших), «Дольше всех лидировал» (max дней в `ChampionReign`, см. ниже — не показывается, если фича боссфайта ни разу не активировалась), «Больше всего защит трона подряд» (`most_boss_fight_defenses` в utils.py — все боссфайты чемпиона внутри одного `ChampionReign` по конструкции победы, т.к. поражение немедленно закрыло бы правление, поэтому достаточно посчитать их число), «Самый долгий боссфайт» (та же метрика — число партий, — что у «Самого длинного матча», отфильтрована по `is_boss_fight`), «Нагибатор клуба» (самое одностороннее очное противостояние — доминирующий выиграл ≥3 раза и ведёт по счёту, ничьи не в счёт), «В ударе сейчас» (текущая активная серия побед, от 2 подряд — отличать от «Лучшая серия побед» = исторический максимум), длинные и самые быстрые матчи (по `accepted_at → completed_at`, матчи без `accepted_at` пропускаются без падения), апсеты — **боссфайты исключены** (иначе ×2-множитель искажал бы рекорд предсказуемой победой), эпичный матч, «Больше всего ничьих» (от 3), «Самый жаркий день клуба» (сумма матчей всех игроков за один день по МСК, от 3 — не путать с «Маньячелло»/«Неистого», это клубный, а не персональный рекорд). **4 новых рекорда (v2.108.0):** «Самое короткое правление» (`shortest_champion_reign`, utils.py — антипод «Дольше всех лидировал», считает только ЗАКРЫТЫЕ правления: текущее незакрытое ещё растёт, сравнивать его как «короткое» нечестно), «Больше всего восхождений на трон» (`most_throne_ascensions` — число ОТДЕЛЬНЫХ периодов `ChampionReign` у игрока, не длительность; единственное восхождение не рекорд, нужно ≥2), «Долгожданная месть» (`longest_awaited_revenge` — самый большой разрыв по времени между поражением и следующей победой над ТЕМ ЖЕ соперником; промежуточные матчи с другими соперниками разрыв не прерывают, порог 7 дней — иначе «на следующий день отыгрался» тоже считался бы «долгожданной»), «Железные нервы» (`steadiest_career` — наименьший разброс рейтинга за ВСЮ карьеру, антипод «Американских горок» из периодических дайджестов, но не за период, и нормировано на число матчей делением на `NEWCOMER_THRESHOLD`+ — иначе игрок с короткой историей искусственно выглядел бы «стабильнее» просто за счёт меньшей выборки).
- **Скрытые ачивки** (`Achievement.hidden`) — 24 самых «сюрпризных» ситуативных ачивок (`phoenix`, `david_goliath`, `fatality`, `no_sweat`, `revenge`, `dominator`, `comeback`, `titans`, `takova_zhis`, `terminator_slain`, `night_king`, `throne_defended`, `throne_denied`, `chance_blown`, `night_owl`, `deuce_storm`, `no_rest_win`, `round_hundred`, `absolute_zero`, `weekend_warrior`, `rock_bottom`, `full_circle_week`, `draw_double`, `first_crown`) не раскрывают имя/условие, пока не заработаны — на экране «Достижения» показываются как «🔒 ???». Реализовано в `_render_achievements` (profile.py) — с v2.98.0 пустая строка между КАЖДЫМ пунктом внутри категории (не только между категориями): плотный список из 43 длинных строк читался тяжело даже разбитым на 6 категорий; худший случай (все ачивки заработаны) всё равно укладывается в лимит Telegram на сообщение (4096 символов) с запасом, см. `test_render_achievements_stays_under_telegram_limit`. Прогресс-бар «Цель: ...» использует отдельный белый список только счётных ачивок и никогда не ссылается на скрытые — утечки не будет.
- **6 новых ситуативных ачивок** (v2.93.0, все скрытые): `absolute_zero` (КАЖДАЯ партия матча 11:0), `weekend_warrior` (победа в субботу/воскресенье по МСК), `rock_bottom` (рейтинг проигравшего упал ровно до 900.0 — пол ветерана), `full_circle_week` (обыграл каждого игрока клуба за трейлинг 7 дней от текущего матча), `draw_double` (2 ничьи подряд, необязательно с одним соперником), `first_crown` (выиграл свой самый первый боссфайт в карьере, независимо от исхода трона). Бэкфиллятся все, **кроме** `rock_bottom` — та же причина, что у `round_hundred`/`highlander`: нужен рейтинг на момент КОНКРЕТНОГО исторического матча.
- **Категории ачивок** (`Achievement.category`, с v2.90.0) — экран «Достижения» сгруппирован по 6 категориям (`CATEGORY_ORDER` в achievements.py: Старт карьеры → Серии → Особые победы → Объём и вехи → Дух клуба → Трон), внутри категории сначала полученные, потом невыполненные — плоский список 36 пунктов подряд читался как нечитаемая простыня (тот же принцип, что у «Статистики» и «Рекордов клуба»).
- **Личные рекорды** (`bot/services/personal_records.py`, v2.100.0) — не ачивка (фиксированный порог) и не клубный рекорд (сравнение между игроками): сравнение всегда только с собственной историей игрока. Не хранятся в БД — считаются заново из истории матчей на каждое подтверждение результата, тем же способом, что и скрытые ачивки. 7 метрик: серия побед подряд, серия без поражений (победа/ничья, рвётся только поражением), побед за один день (по МСК), разгром по очкам в победе (сумма очков победитель−проигравший по всем партиям), самый долгий матч (любой исход — есть у победителя, проигравшего И обоих при ничье), больше всего очков за матч (тоже любой исход), серия побед подряд именно над ОДНИМ конкретным соперником (максимум по всем оппонентам, не общий стрик). Правило порогов одно на все семь: `before > 0 and now > before` — не празднуем самое первое измерение (0 «до» означает «этого события ещё ни разу не было», не «плохой результат»), любое улучшение после — уже рекорд, сколь угодно маленькое; никаких магических чисел на метрику. Апсет (самый смелый разрыв рейтинга, преодолённый в победе) сознательно НЕ включён — единственная из рассмотренных метрик, для которой нет исторических данных для пересчёта задним числом (прошлые рейтинги соперников на момент КАЖДОГО матча не хранятся), потребовала бы отдельного поля в БД в отличие от остальных шести.
- **4 новые ситуативные ачивки** (v2.90.0, все скрытые): `night_owl` (победа ночью 0:00–6:00 МСК), `deuce_storm` (КАЖДАЯ партия матча на дьюсе, не только решающая), `no_rest_win` (победа в матче, начатом в течение 10 минут после предыдущего с тем же соперником — переиспользует h2h-запрос, уже загруженную для `revenge`), `round_hundred` (рейтинг победителя стал ровно кратен 100). Бэкфиллятся при апгрейде через `backfill_achievements`, **кроме** `round_hundred` — та же причина, что у `highlander`/`david_goliath`/`revenge`/`throne_denied`: нужен рейтинг игрока на момент КОНКРЕТНОГО исторического матча, а не текущее значение; будет начисляться только в реальном времени.
- **`highlander`** («Останется только один») перепривязана с v2.84.0: было «рейтинг выше всех по очкам», стало «владеет местом #1» (`Player.is_champion`) — согласовано с тем, что #1 больше не занимается по очкам, только через босс-файт.
- **`peak_rating` обновляется при любом росте рейтинга** — и при победе, и **при ничьей** (андердог может вырасти на ничьей; до v2.61.0 ветка ничьей пик не обновляла — баг занижал рекорд «Высший рейтинг в истории»). Питает этот рекорд. Стартовое значение = рейтинг при регистрации.
- **Экран «Сегодня»** (`show_today_stats`) — общий счёт дня по всем игрокам + личный мини-итог зрителя (его W–L–D и дельта рейтинга за день). **Личная дельта считается только по матчам с участием зрителя** — `match_rating_delta` для не-участника возвращает `-rating_change`, суммировать по всем матчам дня нельзя.
- **Статистика/профиль (v2.94.0)** — 4 новых блока в `_compute_player_stats`/`_render_stats_lines` (общие для личной статистики и публичного профиля): «За 30 дней» (тренд, отдельно от «за карьеру» — тот копится бесконечно и не показывает текущую динамику), «Партий на дьюсе» (сколько партий дошло до 10:10+ и сколько выиграно). Плюс два новых кросс-игровых хелпера, вызываемых отдельно (нужен доступ к чужим рейтингам/чемпиону, не влезают в чистый `_compute_player_stats`): `_rank_gap_line()` — разрыв до соседа сверху по таблице (при пиннинге чемпиона разрыв может быть ≤0 — строка тогда не показывается, чтобы не путать «отрицательным разрывом»); `_throne_distance_line()` — статус в боссфайт-механике («вызови чемпиона» / «до трона: −X pts» / «не хватает матчей» / «впереди <претендент>»), `None` если боссфайт не активирован. **«Умные советы» (v2.109.0)** — 4 персональных инсайта, ещё один блок в `_compute_player_stats`/`_render_stats_lines`, каждый показывается только при достаточной выборке (тот же принцип порогов, что и у остального в этой функции): «Счастливый день» (винрейт по дню недели, минимум 3 матча на день — НЕ путать с уже существующим «Активный день», тот про число сыгранных матчей, не про винрейт), момент-анализ (винрейт в матче сразу ПОСЛЕ поражения, от 3 матчей, разная фраза выше/ниже 50%), «Любимый счёт» (самый частый точный счёт ВЫИГРАННОЙ партии, от 2 повторов), спринтер vs марафонец (винрейт в коротких матчах на 1 партию vs длинных на 3+, показывается только при разнице ≥15 п.п. и минимум 3 матча в каждой группе — иначе почти всегда есть небольшая разница, но она неинтересна).
- **Карьер-рекап** (`show_career_recap`, profile.py, v2.109.0) — кнопка «🎬 Моя история» на «Статистике», доступна в любой момент (не раз в год, как «Итоги года»). Highlight reel, собранный ИЗ уже посчитанных `_compute_player_stats()` полей — не вводит новой логики подсчёта рейтинга/серий. Два новых источника: число заработанных ачивок (`get_achievements`) и число УНИКАЛЬНЫХ покорённых личных рекордов (`PersonalRecordEarned`, `DISTINCT` по `metric` — метрику можно бить многократно за карьеру, но в рекапе интересно «сколько из 7», не «сколько раз»). Сознательно не считает точную дату пика рейтинга — точный replay истории рейтинга потребовал бы дублировать логику применения «пола» рейтинга (900/1000) на каждом шаге, иначе результат разошёлся бы с реальной историей; показывается просто значение `peak_rating`, как и везде в проекте.
- **Мгновенные вехи по матчам/очкам** (`_check_and_send_milestones`, match_result.py, v2.110.0) — НЕ ачивка: разовый пинг сразу после матча на круглых цифрах (каждые 100 матчей, каждые 500 очков за карьеру), отдельно от постоянной коллекции достижений. Вызывается для ОБОИХ участников в обеих ветках `confirm_result` (победа/поражение и ничья одинаково растят оба счётчика у обоих). Переиспользует уже существующие `get_career_matches()` (utils.py) и `_career_points_and_sets()` (achievements.py) — не вводит третью копию подсчёта очков за карьеру (та же логика уже дублируется в `_compute_player_stats`/services/stats.py и achievements.py; здесь сознательно переиспользован achievements-хелпер, а не заведена четвёртая копия). Вехи, которые уже празднует постоянная ачивка (500/1000 матчей — «Стахановец»/«Суперстар»; 4000/8000/12000 очков — «Копил по очку»/«Крепкий середняк»/«Нафармил очков»), пропускаются — иначе одно и то же событие поздравлялось бы дважды подряд. Для очков используется пересечение границы (`points_before // 500 != points_after // 500`), а не точное попадание — один матч обычно даёт 10-90+ очков и почти никогда не попадает ровно на кратное 500; для матчей достаточно точного попадания по модулю, поскольку счётчик матчей растёт строго на 1 за раз и не может «перепрыгнуть» через круглую цифру.
- **«Трон трещит»** (`check_throne_cracking`, scheduler.py, v2.111.0) — часовая джоба (тот же `IntervalTrigger(hours=1)`, что у напоминаний о матчах и авто-освобождения трона), приватное уведомление ТОЛЬКО чемпиону, если кто-то подбирается к нему СНИЗУ ближе чем на `THRONE_CRACKING_GAP`=20 pts. **Принципиально не `get_challenger()`** — та функция возвращает официального претендента, чей рейтинг УЖЕ строго выше чемпионского (боссфайт уже действует на его будущие матчи); «Трон трещит» — заблаговременное предупреждение ДО этого момента, про игрока ещё НИЖЕ чемпиона, который вот-вот сам станет официальным претендентом. Порог `NEWCOMER_THRESHOLD` матчей — та же планка, что и у права на статус претендента, иначе предупреждало бы про случайного новичка с парой матчей. Уведомляет КАЖДЫЙ раз, когда джоба видит разрыв ниже порога, без отдельного флага «уже предупреждали» в БД — если разрыв то расширяется, то снова сужается, чемпион может получить несколько уведомлений подряд по разным часам; сознательно принятый компромисс простоты (обсуждено с пользователем — доп. поле на `Player` ради дедупликации сочли лишней сложностью для 5-игрокового клуба).
- **Итоги дня** (`send_daily_summary`, 21:30 МСК) — broadcast всем. «Топ дня» с полоской формы (исходы последних 7 матчей квадратами 🟩🟥🟨 — считается общим хелпером `_compute_player_form()`, с v2.104.0 используется также в топе недели/месяца), «Лучший рост»/«Отрицательный рост», «Нагибатель дня» (макс серия побед за день), «Чаще всего самбовались» (самая играющая пара за день), «Без поражений дня», «Американские горки дня» (см. ниже), «матч дня», и общий **«Матчи дня»** — лог всех матчей со счётом (нейтрально, победитель жирным). Строка «Сыграно» включает и очки (не только матчи/партии).
- **Итоги недели** (`send_weekly_digest`, пн 9:00 МСК) — персональное сообщение: личная шапка (твой счёт за неделю, рейтинг, место, **прогресс до ближайшей ачивки** — та же логика, что в личной статистике, `_nearest_achievement_progress`/`_compute_player_stats` импортированы из profile.py) + общий клубный блок «Топ недели» (стендинг + полоска формы с v2.104.0, **вместо** прежнего длинного списка матчей) + «Герои недели» (включая «Халявщик(и) недели» — зарегистрированные игроки с историей матчей, которые не сыграли на этой неделе; пустые новички не в счёт) + «Матч недели». **Итоги месяца** (`send_monthly_summary`) — с v2.104.0 персонализирован по тому же паттерну, что и неделя (личная шапка + общий клубный блок), а не единый бродкаст-текст всем игрокам, каким был раньше (решение v2.91.0 «БЕЗ новых метрик для месяца» пересмотрено по итогам живой обратной связи — месяц ощущался суше дня/недели). Несёт полоску формы в топе, «Халявщик месяца», «Без поражений», «Американские горки» — те же хелперы, что и в дне/неделе. **Итоги квартала** (`send_quarterly_summary`, v2.98.0) — 1-го числа января/апреля/июля/октября в 10:30 МСК (`CronTrigger(month="1,4,7,10", day=1, ...)`), самая насыщенная периодическая сводка («итог сезона» в духе Spotify Wrapped — реже, но объёмнее): топ квартала, лучший/отрицательный рост, главный теннисист, дерби, лучшая серия, «без поражений» и «американские горки» (переиспользуют уже period-agnostic `_longest_no_loss_streak`/`_biggest_swing`, см. ниже), суммарные очки, топ-матч квартала. Границы квартала — `_quarter_bounds_msk()`: три календарных месяца перед текущим (джоба стартует 1-го числа, «завершившийся квартал» всегда однозначен).

**«Без поражений»/«Американские горки» без суффикса периода** (v2.97.0) — `_longest_no_loss_streak`/`_biggest_swing` (scheduler.py) больше не принимают параметр `period`: раньше показывали «... дня»/«... недели», хотя заголовок сообщения («Итоги дня»/«Итоги недели») уже задаёт период — повтор в каждой строке был избыточным и непоследовательным с соседними строками того же дайджеста. Как побочный эффект теперь переиспользуются в «Итогах квартала» без правок.

**«Топ-матч дня»/«Все матчи»** (v2.98.0, было «Матч дня»/«Матчи дня») — переименовано в `send_daily_summary`: два похожих по названию заголовка в одном сообщении путали, какой из них что показывает (один — куратор-пик самого драматичного матча, другой — полный лог всех матчей дня). «Матч недели»/«Матч месяца» не переименованы — там нет соседнего «Матчи недели»/«Матчи месяца», путаницы не было.
- **Итоги года** (`send_yearly_summary`, v2.101.0) — 31 декабря в 14:00 МСК (`CronTrigger(month=12, day=31, hour=14, ...)`), намеренно ДО Нового года, а не после. Границы — `_year_bounds_msk()`: от 1 января 00:00 МСК до момента запуска джобы (не до 1 января следующего года — оставшиеся ~10 часов года сознательно не ждём, это срез «по состоянию на 31 декабря», а не строго закрытый календарный год). Самая насыщенная и намеренно многословная сводка из всех — единственное сознательное исключение из правила «не простыня, режь на группы» (см. остальные экраны проекта): это письмо раз в год, а не экран, который открывают на бегу. Полный табель клуба (не топ-3, а все игроки, как и в квартальной), чемпион года (текущий чемпион на момент отправки, не анализ смены трона за год), взлёт/падение года, нагибатор года, дерби года, «без поражений»/«американские горки» года, топ-матч года — плюс к каждому из этих разделов приклеена ОДНА конкретная зафиксированная фраза-отсылка (не пул со случайным выбором, как у пасхалок — согласовано с пользователем построчно). Сознательно не считает, сколько ачивок/личных рекордов разблокировано за год — ни ачивки, ни личные рекорды не хранят дату получения, посчитать за период невозможно без новой инфраструктуры (не выйти было бы за рамки уже одобренного скоупа).
- **Звания, зал славы, карточка победы, постоянная клавиатура** (v2.102.0). **Звания** (`rank_title`, utils.py) — чисто косметическая IT-тематическая строка по диапазону рейтинга (границы пересмотрены в v2.103.0, см. ниже), не влияет ни на рейтинг, ни на матчмейкинг, ни на боссфайт — выводится рядом с рейтингом в личной статистике и публичном профиле. **Зал славы** (`show_hall_of_fame`, leaderboard.py) — кнопка на экране «Рейтинг», рядом с «Индексом формы»: полный список всех периодов `ChampionReign` (новейший сверху), текущее правление помечено «— сейчас»; если боссфайт ни разу не активировался — список пуст, отдельный текст без падения. **Оживление (v2.108.0):** шапка над списком с парой фактов — сколько всего было смен трона + самое короткое правление (та же метрика, что и в «Рекордах клуба», здесь для контекста прямо над списком). К каждому ЗАКРЫТОМУ правлению — нарратив «как закончилось» (`_reign_end_narrative`): матч смены трона ищется по совпадению `Match.completed_at` с `ChampionReign.ended_at` (их связывает `try_transfer_champion(at=match.completed_at)` в match_result.py) — если найден боссфайт с этим временем и `winner_id` не совпадает с бывшим чемпионом, нарратив собирается через уже готовый `match_report()`; если такого матча нет — трон освободился автоматически (`check_champion_auto_release`, scheduler.py), текст «Трон отошёл без боя — чемпион давно не защищался». **Карточка «поделиться победой»** (`send_share_card`, match_result.py) — кнопка `share_card_{match_id}` появляется только у РЕАЛЬНОГО победителя (не у того, кто вводил счёт — если репортёр проиграл, кнопка уходит в уведомление настоящему победителю через `main_menu_kb(share_match_id=...)`, а не в `rematch_kb` репортёра), защищена проверкой владения в хендлере (`viewer.id != match.winner_id` → алерт), недоступна при ничьей. Простой текстовый вариант (счёт + дельта рейтинга + `match_report()`), без рендера изображения — сознательный выбор, чтобы не тащить графическую библиотеку ради MVP. **Постоянная клавиатура** (`main_reply_kb()`, inline.py) — `ReplyKeyboardMarkup` с 3 кнопками в один ряд (Вызвать на матч / Рейтинг / Статистика), отправляется отдельным сообщением после приветствия в `/start` (Telegram не позволяет два разных типа `reply_markup` в одном сообщении). Каждая кнопка — текстовый хендлер-двойник (`F.text == "..."`) поверх уже существующего инлайн-хендлера: чтобы не дублировать логику, три экрана (`_build_leaderboard_screen`/`_build_stats_screen`/`_build_challenge_screen`) вынесены из своих callback-хендлеров в чистые билдеры `(текст, клавиатура)`, вызываемые и из callback (`edit_text`), и из текстового хендлера (`answer`) — паттерн применён только к тем трём экранам, что были нужны кнопкам, остальные не трогали. Роутеры с текстовыми хендлерами-двойниками зарегистрированы в `main.py` раньше `match_result_router`, поэтому FSM-хендлер ввода счёта не перехватывает текст кнопок — совпадение достаточное, отдельного guard-кода не потребовалось.
- **Индекс формы** (`show_form_index`, leaderboard.py, v2.101.0) — кнопка на экране «Рейтинг», рядом с «Рекордами клуба»/«Матрицей доминирования» (тот же паттерн: кнопка → отдельный экран). Ранжирует не по всей карьере (как основной лидерборд), а по последним `FORM_WINDOW`=10 матчам каждого игрока — сортировка по чистой сумме дельты рейтинга в этом окне. Слабый по общему рейтингу игрок на удачной полосе виден в топе этого экрана, хотя на основном лидерборде так не увидеть. Иконки 🔥 (окно ≥+15 pts) / 🥶 (≤−15 pts) / ⚡ (между) — не путать с 🔥/❄️ на лидерборде и экране вызова (те бинарные: стрик/неактивность, не числовая дельта).
- **Репортаж матча** (`match_report()`, utils.py, v2.101.0) — расширяет короткую `match_drama_reason()` в связный абзац для топ-матча периода (используется в «Топ-матч дня»/«Матч недели»/«Матч месяца»/«Топ-матч квартала»/«Топ-матч года» — все 5 периодических дайджестов, и в карточке «поделиться победой»; «Самый эпичный матч» в рекордах клуба сознательно НЕ тронут, там плотный список, длинный абзац туда не подходит). Собирается из фраз-кусочков по 4 факторам (камбэк — проигрывал именно ПЕРВЫЕ ДВЕ партии, строже, чем «камбэк» в `match_drama_reason`, которому хватает одной; марафон 5+ партий; дьюс именно на РЕШАЮЩЕЙ партии, не любой; апсет ≥20 pts), фиксированные фразы, не LLM-генерация на лету. Если ни один из 4 факторов не сработал — пробует `match_drama_reason()`; если и та упала на одну из двух плоских строк («Уверенный разгром»/«Напряжённый матч») — с v2.105.0 подставляется фраза из `BLOWOUT_PHRASES`/`PLAIN_WIN_PHRASES` (по 7 штук, индекс по `match.id`, тот же принцип стабильности, что у `match_phrase`) — на живом скрине пользователь заметил, что плоская строка на карточке победы выглядит скучно. Более содержательные исходы `match_drama_reason()` (дьюс на тоненького, решилось в последней партии) не подменяются — под них фраз-пула нет и не нужно.
- **Контекст прошлой встречи на карточке победы** (`previous_h2h_line()`, utils.py, v2.105.0) — новая строка на карточке «поделиться победой» по исходу ПРЕДЫДУЩЕГО (не текущего) очного матча с этим же соперником, через уже существующий `get_h2h_matches(exclude_match_id=текущий)`: реванш (`H2H_REVENGE_PHRASES`, 6 шт — в прошлый раз проиграл именно этому сопернику), продолжение серии (`H2H_STREAK_PHRASES`, 6 шт — выиграл у него и в прошлый раз), была ничья (`H2H_DRAW_PHRASES`, 3 шт). Если очных матчей раньше не было вообще — строка не показывается (не выдумываем контекст из ничего). Индекс по id ТЕКУЩЕГО матча, не предыдущего — стабильность в рамках повторного показа одной карточки.
- **Даты получения ачивок и личных рекордов** (v2.106.0) — раньше нигде не хранилось, КОГДА игрок получил ачивку или побил личный рекорд, только сам факт (этим объяснялось, почему «Итоги года» не считают ачивки за год). Две новые таблицы: `AchievementEarned` (player_id, achievement_id, earned_at — одна строка на пару, ачивки не переоткрываются) и `PersonalRecordEarned` (player_id, metric, value, match_id, earned_at — полная ИСТОРИЯ, метрику можно бить многократно за карьеру, в отличие от ачивок). **Реал-тайм:** новый хелпер `record_achievements_earned()` (achievements.py) вызывается ПОСЛЕ каждого `check_win/loss/draw_achievements()`/`check_cancel_achievements()`/`check_boss_fight_*()`/`check_chance_blown_achievement()` (6 точек вызова в match_result.py/challenge.py) — сами функции возвращают только список id, без контекста момента, дата (`match.completed_at` для матчевых событий, `datetime.now()` для отмены) передаётся явно вызывающим. Личные рекорды пишутся НЕ через отдельный хелпер, а прямо внутри `check_personal_records_on_win/loss/draw()` (personal_records.py) — эти функции с v2.106.0 возвращают `(сообщение, ключ_метрики, значение)` кортежи внутренне, а наружу по-прежнему отдают только `list[str]` (внешний контракт с match_result.py не изменился). **Бэкфилл существующих ачивок:** `backfill_achievements()` переписан на инкрементальный проход — вместо постфактум-проверок по агрегату (макс.серия, общее число матчей и т.п.) после цикла, вехи теперь считаются ПРЯМО в цикле по матчам (стрики — сравнением `win_streak == N` в момент инкремента, вехи по числу матчей — индексом `matches[N-1]`, вехи по очкам/партиям — инкрementальным счётчиком по той же перспективе, что `_career_points_and_sets`), поэтому дата берётся из конкретного матча-триггера. `BACKFILL_VERSION` 10→11 форсирует один дополнительный проход ДАЖЕ для уже полностью забэкфилленных игроков — иначе их существующие (до этой фичи) ачивки остались бы без дат навсегда. Реконциляция дат — отдельный блок в конце per-player цикла: для каждой ачивки в итоговом `earned` без строки в `AchievementEarned` создаётся строка (дата из `dates`, если поймали в ЭТОМ проходе, иначе `NULL`). Ачивки, которым принципиально нужен снапшот рейтинга на момент КОНКРЕТНОГО исторического матча (`highlander`, `david_goliath`, `revenge`, `throne_denied`, `chance_blown`, `rock_bottom`, `rating_1200`), бэкфилл вообще не проверяет — дата у них остаётся `NULL` и после реконциляции, честно, не выдумываем задним числом. **Бэкфилл личных рекордов:** новый `backfill_personal_records()` (personal_records.py, вызывается из `init_db()` рядом с `backfill_achievements`) — НЕ версионирован (рекорды никогда не хранились на `Player`), запускается один раз в жизни игрока (условие — полное отсутствие строк в `PersonalRecordEarned`, а не отдельный счётчик версии), проходит по матчам хронологически и на каждом шаге сравнивает метрику с максимумом ДО этого матча растущим окном — фиксирует КАЖДОЕ историческое улучшение отдельной строкой (один рекорд можно бить несколько раз за карьеру, в отличие от ачивок). Логика веток по исходу (win/loss/draw) дублирует реал-тайм проверки — тот же принятый компромисс, что у `backfill_achievements()` vs `check_win_achievements()`: растущее окно бэкфилла и «только последний матч» реал-тайма плохо уживаются в одной функции.
- **Тепловая карта активности** (`activity_heatmap_url()`/`activity_counts_by_day()`, utils.py, v2.107.0) — кнопка «🔥 Активность» на «Статистике» (`stats_kb()`), окно `HEATMAP_DAYS`=90 (не год — у клуба несколько месяцев реальной истории, full-year сетка была бы наполовину пустой). **quickchart.io НЕ поддерживает тип графика `matrix`/`heatmap` на публичном инстансе** — проверено вручную запросом к API, ответ: `Chart error: "matrix" is not a registered controller"`. Сетка эмулируется через стандартный `bubble`-chart: x = номер недели, y = день недели (0=Вс снизу..6=Пн сверху), цвет точки = интенсивность (4 тира: 0/1/2–3/4+ матчей). Радиус фиксирован на уровне ДАТАСЕТА (не на каждой точке) — тот же принцип, что у GitHub-графика контрибуций (цвет — сигнал, не размер), и 4 датасета по тиру интенсивности вместо `backgroundColor` на каждую точку — тот же трюк ощутимо короче итоговый URL (доходило до ~8.5КБ с цветом на точку против ~4.1КБ с 4 датасетами при 90 точках, тестировалось вручную перед реализацией). Переключатель личная/клубная — ОДНА точка входа (`activity_heatmap_me`/`activity_heatmap_club`, history.py), кнопка под самой картинкой меняется на противоположную; тот же паттерн «шлём новое фото, удаляем предыдущее по chat_id», что и у графика рейтинга (`_last_chart_msg`), но отдельный dict `_last_activity_msg` — чтобы переключение активности и графика рейтинга в одном чате не затирали message_id друг друга. **Цвета тиров и подписи осей (v2.107.1)** — по живому скриншоту пользователь заметил, что легенда в подписи (везде один 🟩) не совпадает с реальными цветами точек на картинке (3 разных оттенка зелёного), и что без подписей осей неясно, что означают ряды/столбцы. `_HEATMAP_TIER_COLORS` сменены на жёлтый/оранжевый/красный (⬜🟨🟧🟥) — единственный набор из 4 цветов, для которого в Unicode есть готовые квадраты-эмодзи, поэтому легенда и картинка теперь совпадают буквально, не просто «по смыслу». Подписи осей через сам quickchart добавить не удалось — публичный инстанс НЕ выполняет `ticks.callback` как JS-функцию, переданную строкой (проверено вручную: `TypeError: string "function(...)" is not a function`, тот же принцип «тестируем, не предполагаем», что и с `matrix`-типом графика) — решено текстом в подписи к фото: «Ряды сверху вниз: Пн Вт Ср Чт Пт Сб Вс» + «Столбцы слева направо: N дней назад → сегодня».
- **Границы званий подняты на +100, кнопки перебалансированы, 9 новых ачивок** (v2.103.0) — по результатам проверки живых прод-данных: на исходных границах (v2.102.0) уже 2 из 7 игроков клуба были «Ген дир», а новичок с рейтингом 1000 (старт с нуля) сразу оказывался «Миддлом». Новые `RANK_TITLE_BANDS`: Джун <1050 / Миддл 1050–1149 / Сеньор 1150–1249 / Тим лид 1250–1349, `RANK_TITLE_TOP` = Ген дир (1350+) — только реальный лидер клуба, новичок стартует «Джуном». **Кнопка «Сегодня»** перенесена с экрана «Рейтинг» на «Статистику» (`leaderboard_kb`→`stats_kb`, inline.py) — на «Рейтинге» было 5 доп.кнопок (Рекорды/Матрица/Индекс формы/Зал славы/Сегодня) против 3 на «Статистике», а у «Сегодня» уже есть встроенный личный блок («твой W-L-D за день»), что ближе к «моя статистика», чем к клубному сравнению игроков. `show_today_stats` (leaderboard.py) теперь возвращает на `back_to_stats_kb()` (новый хелпер, inline.py), а не `back_to_leaderboard_kb()` — иначе «« К рейтингу» вела бы не туда, откуда пришёл пользователь. **9 новых ачивок-вех** (все в `CAT_MILESTONES`, не скрытые, `BACKFILL_VERSION` 9→10 — кумулятивные метрики полностью восстановимы из истории, в отличие от `highlander`/`rock_bottom` и т.п.): по числу матчей — `workhorse` (500, «Стахановец»), `monument` (750, «Монумент»), `superstar` (1000, «Суперстар»); по очкам за карьеру — `point_saver` (4000, «Копил по очку»), `sturdy_grinder` (8000, «Крепкий середняк»), `point_farmer` (12000, «Нафармил очков»); по выигранным партиям за карьеру — `set_sniper` (200, «Сетовый снайпёр»), `set_veteran` (500, «Сетовый ветеран труда»), `set_legend` (1000, «Сетовая лехенда»). Новый хелпер `_career_points_and_sets()` (achievements.py) считает очки/выигранные партии с перспективы игрока (та же перспектива, что в `_match_line`/`_compute_player_stats`) — используется во всех 4 местах, где раньше проверялись только «Вехи по числу матчей» (`check_win_achievements`/`check_loss_achievements`/`check_draw_achievements`/`backfill_achievements`). `_compute_player_stats` (services/stats.py) теперь дополнительно возвращает `sets_won` (раньше считался только внутри функции для `sets_win_rate`, наружу не отдавался) и новый `career_points` — оба питают эти вехи в прогресс-баре «Цель: ...» (`_nearest_achievement_progress`).
- **Единые названия метрик** во всех сводках: пара «📈 Лучший рост» / «📉 Отрицательный рост» (день/неделя/месяц). «Чаще всего самбовались» (дерби периода) и «Нагибатель периода» считаются общими хелперами `_most_played_pair` / `_longest_streak` в scheduler.py. С v2.91.0 туда же добавлены `_longest_no_loss_streak` («Без поражений» — серия без поражений, ничья её НЕ обнуляет, в отличие от `_longest_streak`) и `_biggest_swing` («Американские горки» — у кого рейтинг сильнее всего мотало туда-обратно за период: сумма |дельт| минус |итоговая дельта|, порог 20 pts — метрика волатильности пути, а не чистого net-результата, который уже показывают «Лучший рост»/«Отрицательный рост»). Обе используются в дне и неделе, не в месяце.
- **Таймзона cron-задач задаётся явно** (`timezone="Europe/Moscow"` в каждом `CronTrigger`, часы — прямо в МСК). `CronTrigger` без аргумента `timezone` берёт ЛОКАЛЬНУЮ tz сервера (`get_localzone()`), а НЕ `timezone` у `AsyncIOScheduler` (тот действует только на триггеры из строки `'cron'`). На сервере в зоне Europe/Amsterdam расписание уезжало на 2 часа (фикс v2.63.1). Матрица доминирования рендерится в блоке `<pre>` (а не инлайн-`<code>`): именно `<pre>` держит моноширинность и горизонтальный скролл, поэтому имена в сетке выравниваются ровно. Подписи игроков — обрезанные имена, длина обрезки подбирается минимальной для уникальности (v2.63.2; в v2.63.1 были номера+легенда — читались хуже).

---

# Рейтинговая формула

```
E                = 1 / (1 + 10 ^ ((loser_rating - winner_rating) / 400))
base_delta       = 32 * (1 - E)

sets_ratio       = winner_sets_won / total_sets        # 0.6..1.0
pts_ratio        = winner_total_pts / all_pts          # ~0.5..0.85
score_mult       = 1 + 0.5*(sets_ratio - 0.5) + 0.3*(pts_ratio - 0.5)

short_match_mult = 0.75 если len(sets_data) == 1, иначе 1.0   # штраф за 1-партийный матч
newcomer_bonus   = 1.2  если у победителя < 15 матчей, иначе 1.0
repeat_mult      = max(0.5, 1.0 - 0.05 × streak)              # streak — подряд побед над тем же соперником

delta = round(base_delta × score_mult × short_match_mult × newcomer_bonus × repeat_mult, 1)
```

Победитель: `+delta`. Проигравший: `-delta`. Пол рейтинга: **1000.0** (новичок, < 15 матчей) / **900.0** (ветеран).

---

# Стек

| Слой | Технология |
|------|-----------|
| Язык | Python 3.13 |
| Telegram | aiogram 3.x, long-polling |
| ORM | SQLAlchemy 2.x async |
| БД | SQLite + aiosqlite |
| FSM storage | MemoryStorage (сбрасывается при рестарте) |
| Фоновые задачи | APScheduler 3.x |
| Хостинг | VPS FirstVDS (Ubuntu 24.04), systemd |
| БД в проде | `/data/bottennis.db` на VPS |
| Конфиг | python-dotenv, `.env` |

---

# Архитектурные решения

- **MemoryStorage для FSM** — сознательный выбор для MVP. При рестарте бота незавершённые вводы результата сбрасываются. Приемлемо для малой группы.
- **DatabaseMiddleware делает commit после каждого хендлера** — хендлеры тоже делают явный commit там, где нужно промежуточное сохранение. Двойной commit безопасен с SQLAlchemy.
- **SQLite в проде** — для небольшой закрытой группы достаточно. При росте нагрузки — миграция на PostgreSQL.
- **Инвайт-код через deep-link** — `/start CODE`. Если `INVITE_CODE` не задан в `.env`, регистрация открыта для всех.

---

# Соглашения

- Все сообщения бота — на **русском языке**, HTML parse_mode
- Имена пользователей всегда экранировать: `from html import escape as h`
- Callback data формат: `action_id` или `action_subaction_id` (например `cancel_match_42`)
- Новый функционал → новый хендлер в подходящем роутере, не в `start.py`
- Запрещено: хардкод токенов, Markdown вместо HTML, `int(rating)` вместо `round(rating, 1)`
- **Описания релизов** (GitHub Release и новые записи `RELEASE_NOTES.md`) — **без эмодзи**, сухо и по делу, как в реальном инженерном проекте. Только суть изменений: что сделано, почему, что затронуто. Никаких смайлов, восклицаний и «воды».

---

# После каждого релиза — обязательная база (всегда, без исключений)

1. **Проверить на баги** — пройтись по изменённому коду и смежным местам, найти и исправить проблемы
2. **Обновить RELEASE_NOTES.md** — новая версия с датой и описанием
3. **Записать в Mionika** — синхронизировать граф знаний (см. ниже)

> **Mionika** — персональная база знаний для AI-агентов в виде графа: решения, заметки, задачи и связи между ними. Claude (и другие ИИ) читают и пишут туда напрямую через MCP, не теряя контекст между сессиями. Если у тебя её нет — просто пропусти все шаги про Mionika, на работу бота это не влияет.

---

# После каждого изменения (детали)

1. Обновить **RELEASE_NOTES.md** — новая версия с датой и описанием
2. Обновить **README.md** — если изменился стек, структура или список фич
3. Обновить **Mionika** (projectId: `tennisbot`) — синхронизировать граф знаний:
   - Новая фича → добавить узел в нужный раздел (стек / архитектура / бизнес-правила)
   - Новое архитектурное решение → создать узел типа `decision`
   - Изменилось бизнес-правило → обновить существующий узел через `batch_crud` (update_atom)
   - Изменилась формула или алгоритм → обновить узел в разделе "Рейтинговая формула ELO"
   - Исправлен баг с последствиями для логики → добавить заметку типа `note` с описанием что было не так и как исправлено

# Деплой — порядок команд (обязательно!)

Перед каждым деплоем сначала прогоняем тесты, потом деплоим:

```bash
# 1. Локально — прогоняем тесты
py -3.13 -m pytest tests/ -v

# 2. Пушим в GitHub (develop → PR → main)

# 3. Деплой — автоматический после мержа в main.
#    Запасной ручной вариант (IP заменить на актуальный после переезда):
ssh root@НОВЫЙ_IP "cd /opt/bottennis && git pull && systemctl restart bottennis"
```

Если тесты упали — деплой не делаем, сначала чиним.

**⚠️ Заметка про PyPI:** на старом VPS PyPI был заблокирован провайдером — использовалось зеркало `https://pypi.tuna.tsinghua.edu.cn/simple/`. На новом сервере сначала проверить прямой доступ, зеркало — только если понадобится.

---

# Аудитория и поведение пользователей

- **5 активных игроков**, все в одном офисе
- **3–5 матчей в день** на человека, стол один — матчи строго последовательные
- **Вызов сначала голосом** («будешь играть?»), потом через бота — поэтому напоминания о матчах и вызовах практически не нужны
- **Реванш** бывает, но нечасто
- **Споров о счёте не бывает** — доверяют друг другу
- **Лидерборд смотрят просто для интереса**, не только перед вызовом
- **Только рабочее время**, вечера и выходные почти не играют
- **Сезонов и турниров нет** — вечный рейтинг, сбрасывать не нужно
- **Еженедельный дайджест** — реакция пока неизвестна

---

# Текущий статус (август 2026)

**Статус проекта:** активная разработка. Проект живой и дорабатывается — новые фичи, UX-правки, багфиксы. Никакого фич-фриза (был maintenance mode с v2.63.2, снят в июле 2026). Идеи обсуждаем и делаем, а не складываем в backlog по умолчанию.
**Готово:** всё перечисленное в разделе "Что умеет бот" (включая боссфайт за 1-е место, v2.84.0, и его харденинг по итогам код-ревью, v2.85.0). В `main` и проде — v2.107.1. В `develop` — весь брейншторм-заход от 2026-09-03 целиком: v2.108.0 (рекорды клуба + Зал славы), v2.109.0 (умные советы + карьер-рекап), v2.110.0 (вехи по матчам/очкам), v2.111.0 («Трон трещит», в работе) — по завершении уйдёт в `main` одним релизным PR; 564 тестов, CI зелёный.
**Хостинг:** ✅ переезд завершён — бот живой на новом сервере FirstVDS (Нидерланды), systemd-сервис `bottennis`, БД в `/data/bottennis.db`. BOT_TOKEN перевыпущен, секреты CI обновлены под новый сервер.
**Автодеплой:** ✅ с v2.55.0 — job `deploy` в `.github/workflows/tests.yml`. Мерж PR `develop→main` → при зелёных `lint`+`pytest` job по SSH делает `git pull --ff-only` + `systemctl restart bottennis`. Секреты репозитория: `VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY` (выделенный ed25519 deploy-ключ — доступ GitHub Actions → VPS). С 2026-09-02 `git pull --ff-only` явно проверяется на ошибку (`set -e` + `if ! git pull...; then exit 1`) — раньше при падении команды скрипт молча ехал дальше и рестартовал сервис со старым кодом, а джоба всё равно светилась зелёной (см. «Устранено» в конце файла).
**Авторелиз:** ✅ — job `release` в том же workflow при мерже в `main` создаёт GitHub Release из верхней секции `RELEASE_NOTES.md` (идемпотентно, по тегу).
**Не реализовано:** PostgreSQL (при миграции учесть: `telegram_id` сейчас `Integer` — для PG нужен `BigInteger`).

# Запланировано на будущее

- При росте нагрузки/группы: PostgreSQL (+ `BigInteger` для `telegram_id`). Не сейчас — для 5-6 игроков SQLite с запасом.
- Гигиена: периодически повторять проверку, что offsite-бэкап реально восстанавливается — последний раз проверено 2026-09-02 (скачан файл с прода, открыт локально как обычная SQLite-база, `PRAGMA integrity_check` → ok, счётчики игроков/матчей совпали с живым продом). Заодно раз в полгода `pip list --outdated` (см. дальше по тексту) — последний раз проверено там же: ничего критичного, но `aiogram` отстал сильнее всех (17 minor-версий), стоит держать в уме на будущий апгрейд.
- **Dependabot — рассмотрели и сознательно отложили** (2026-07-13). У проекта всего 6 прямых зависимостей, закрытый бот без публичного HTTP-интерфейса и без денег на кону — площадь атаки маленькая, а автоматические PR на апдейты создают ревью-нагрузку почти без пользы на таком масштабе. Раз в полгода вручную `pip list --outdated` достаточно. Если размер команды/риски вырастут — конфиг: `interval: "monthly"` + группировка всех апдейтов в один PR (`groups: patterns: ["*"]`), отдельно `pip` и `github-actions`, файл `.github/dependabot.yml` (читается из `main`).

**Устранено (2026-09-02):** релиз v2.107.0 вживую показал, что `deploy`-job могла быть зелёной, а прод при этом оставался на старой версии. Причина — VPS клонировал репозиторий по анонимному HTTPS (`https://github.com/...`), а GitHub на аутентифицированном шаге `git-upload-pack` (сама передача пакета объектов, не первичный ref listing — тот проходил на 200 без проблем) стал отвечать `401 Unauthorized` именно с IP этого VPS (похоже на антиабьюз-эвристику по дата-центровым адресам — с обычной домашней/рабочей сети то же самое клонирование проходит штатно). При этом `git pull --ff-only` в скрипте деплоя ничем не был защищён — при его падении bash-скрипт (`appleboy/ssh-action`) просто ехал на следующую строку, `systemctl restart bottennis` рестартовал сервис со СТАРЫМ кодом (working tree не менялся), встроенная проверка «сервис активен после рестарта» это не ловила (сервис-то активен, просто не то запустил), и джоба репортила успех. **Фикс — два независимых слоя:** (1) `origin` на VPS переключён с анонимного HTTPS на SSH через отдельный read-only deploy key (`~/.ssh/bottennis_deploy_key`, добавлен в GitHub repo → Settings → Deploy keys без права записи) — тот же git-протокол, что более надёжно проходит с дата-центровых IP, не завязан на анти-абьюз эвристику анонимного HTTPS; (2) в `.github/workflows/tests.yml` деплой-скрипт получил `set -e` и явную проверку кода возврата `git pull --ff-only` с `echo "::error::..."` — теперь при повторении подобной проблемы (любой) джоба падает явно и прод остаётся на предыдущей рабочей версии, а не тихо доезжает до рестарта со старым кодом под зелёной галочкой. Экстренный ручной синк на момент инцидента (пока deploy key ещё не был добавлен) сделан через `git bundle` (полный набор объектов создан локально, перенесён на VPS через `pscp`, применён `git fetch <bundle> main:<tmp-ref>` + `git merge --ff-only`) — обходит сеть до GitHub полностью, безопасный способ синхронизировать git-историю без доверенного канала до origin.

# Известные ограничения (сознательно не чиним сейчас)

- **Гонка в правиле «один активный матч одновременно»** (`send_challenge`, `bot/handlers/challenge.py`, с v2.79.0). Проверка «нет ли уже активного матча» (`get_active_match`) и создание нового `Match` — два отдельных шага без атомарной защиты (в отличие от CAS-guard в `confirm_result`/`do_cancel_match`). aiogram по умолчанию обрабатывает апдейты параллельными asyncio-тасками (в `main.py` нет `handle_as_tasks=False`), поэтому теоретически два почти одновременных вызова (двойной тап, или два игрока вызывают друг друга в одну секунду) могут оба пройти проверку до того, как первый `INSERT` закоммитится — у игрока окажется два активных матча. Осознанно не чиним: вероятность на 5-6 игроках низкая, а последствия смягчены существующим кодом — `handle_direct_score` (match_result.py) уже штатно обрабатывает случай «2+ активных матча» (просит выбрать через меню, есть регресс-тест). Правильный фикс — атомарная перепроверка после INSERT с откатом при конфликте — трогает свежепереписанный и хорошо покрытый тестами флоу ради редкого сценария без хардового последствия. Если группа вырастет или баг реально всплывёт — тогда чинить прицельно. Босс-файт (v2.84.0) создаётся через ту же `send_challenge` и наследует эту гонку — сознательно не чинили заодно, по той же причине.

**Устранено (v2.93.0):** `confirm_result` разрослась была до ~480 строк в одной функции. Разбита на именованные корутины без изменения поведения — `_handle_boss_fight_outcome` (перенос трона + клубное уведомление + ачивки защиты/поражения претендента), `_award_win_achievements_and_eggs`/`_award_draw_achievements_and_eggs` (ачивки + пасхалки для соответствующей ветки), `_notify_challenger_status_change` (уведомления о смене претендента — «обошёл чемпиона» / «Просран шанс»). Расчёт рейтинга и мутация `Match`/`Player` намеренно оставлены прямо в `confirm_result` — самая чувствительная часть, лишний раз не трогали. `confirm_result` сократилась до ~310 строк. Поведение подтверждено полным прогоном тестов (381/381 зелёных) до и после — без единого изменения ассертов.

**Устранено (v2.95.0/v2.96.0):** уборка находок `/code-review high` от 2026-08-19 (задокументированных, но сознательно не исправленных сразу в v2.94.2 — риск регрессии в чувствительном коде сразу после рефакторинга был важнее, чем скорость). Два отдельных захода: **заход 1** (v2.95.0, низкий риск) — общие `_ru_plural`/`as_naive`/`msk_hour_and_weekday`/`rating_tenths` в utils.py вместо 11 копий одной и той же логики (5× склонение, 3× tz-защита, 3× проверка «круглого» рейтинга), `full_circle_week` в бэкфилле O(n²) → O(n) через скользящее окно, `_compute_player_stats`/`_nearest_achievement_progress` вынесены в `bot/services/stats.py`. **Заход 2** (v2.96.0, самая чувствительная часть) — h2h-история между парой запрашивалась 4 раза за одно подтверждение победы (`check_win_achievements`, `_collect_egg_context`, `_send_quick_rematch_egg`, серия побед подряд); вынесена в общий `get_h2h_matches()`, запрашивается один раз в `_award_win_achievements_and_eggs`. Сделан отдельным PR после захода 1 (не сразу после свежего рефакторинга той же функции), с полным прогоном тестов на каждом шаге. 399 → 407 тестов.
